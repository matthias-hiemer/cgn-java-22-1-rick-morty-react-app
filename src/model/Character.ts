import {Origin} from "./Origin";

export type Character = {
    id : number,
    name : string,
    status : string,
    image : string,
    origin : Origin,
    gender: string,
    species: string
}
